---
title: Home
---

import LoadedMap from '../components/LoadedMap'
import { mapSettings } from '../data/settings.js'
import LoadedNeighborhoods from '../components/LoadedNeighborhoods'
import LoadedMBTA from '../components/LoadedMBTA'

# Boston, In Fifteen Minutes

*An analysis of accessibility in Boston.*

## Neighborhoods

Shown on the map below are the census tracts of Boston, which will be the basis of our data analysis.

Currently, these census tracts are color-coded by neighborhood.

<LoadedMap {...mapSettings}>
    <LoadedNeighborhoods />
</LoadedMap>

## MBTA 
<LoadedMap {...mapSettings}>
    <LoadedMBTA />
</LoadedMap>